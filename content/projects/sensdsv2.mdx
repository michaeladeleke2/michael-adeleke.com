---
title: "SensDSv2"
summary: "A desktop application that turns a 60 GHz radar sensor into a machine learning classroom, with live spectrograms, gesture collection, model training, and prediction in one loop a student can run in a single class period."
period: "2025 – present"
area: "Radar sensing and ML education"
role: "Lead developer and researcher"
order: 1
featured: true
stack:
  - "PyQt6"
  - "Infineon BGT60TR13C"
  - "PyTorch"
  - "Vision Transformers"
  - "pyqtgraph"
links:
  - { label: "Under review at SIGCSE", href: "/publications/" }
figures:
  - src: "/research/sensds/visualize"
    width: 1000
    height: 460
    label: "Visualization"
    alt: "A live micro-Doppler spectrogram: a bright zero-Doppler line across the frame with gesture sidebands breaking above and below it."
    caption: "Radar returns become a spectrogram in real time. Time runs left to right, the vertical axis is Doppler velocity, and the bright line through the centre is the static return that a moving hand breaks away from."
  - src: "/research/sensds/collect"
    width: 800
    height: 386
    label: "Collecting data"
    alt: "The Collect Gesture Samples tab, showing a student name, a gesture label, a sample count, and the last captured gesture plotted beside them."
    caption: "Students record their own labelled samples. Each capture is named, counted, and written into the dataset the class will train on, so the training set is the room they are standing in."
  - src: "/research/sensds/train"
    width: 800
    height: 386
    label: "Model training"
    alt: "The Train Model tab, with accuracy and loss plotted per epoch and a training log printing beneath them."
    caption: "A Vision Transformer trains on those captures, with accuracy and loss drawn per epoch and the run log printing alongside. The hyperparameters are exposed rather than hidden in a script."
  - src: "/research/sensds/test"
    width: 800
    height: 390
    label: "Testing"
    alt: "The Test Gestures tab, where a maze game is driven by live gesture predictions with a model-confidence bar chart underneath."
    caption: "The trained model then drives something. Live predictions steer a maze game while per-gesture confidence is shown underneath, so a wrong move is traceable back to the signal that caused it."
---

Machine learning is usually taught with datasets that arrive pre-cleaned and
pre-labeled. The interesting decisions, what to measure, how to represent it,
and what to do when the signal is ambiguous, have already been made by someone
else. SensDS exists to hand those decisions back to the student.

The application drives an Infineon BGT60TR13C 60 GHz radar sensor and takes a
class through the whole loop: watch the micro-Doppler spectrogram move as you
move, record labeled gesture captures, train a model on what the class just
collected, and test it live. Six tabs, one for each stage, make the pipeline
visible rather than hiding it behind a script.

## The signal pipeline

Raw radar frames become spectrograms through a chain the interface exposes
rather than conceals: a range FFT to resolve distance, moving target indication
to suppress static clutter, and then a short-time Fourier transform across slow
time to produce the micro-Doppler image that a Vision Transformer is trained
on. Chirp parameters are configured through the Infineon SDK and surfaced in
the interface, so the relationship between sensor configuration and the
resulting image is something a student can change and see.

## Novelty scoring

Classroom data is not lab data. Students gesture at the wrong distance, at the
wrong angle, or invent a gesture nobody trained on. A PCA-based novelty score
flags captures that fall outside the training feature space. This creates an
opportunity to make distribution shift a thing students encounter directly
rather than a term in a lecture. It also motivates on-device training as a
design target, since the response to "the model has never seen this" is to
retrain on the room you are in.

## Deployment constraints

The system runs on Surface Pro hardware that schools actually have, not on a
workstation. Getting inference to keep up meant reworking model selection and
inference scheduling rather than assuming access to a dedicated GPU. It ships
as signed macOS and Windows executables with a user guide because a tool that
requires a Python environment to be configured is a tool that does not get used
in a classroom.
